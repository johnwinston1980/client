export interface Product {
    id?: string,
    name?: string,
    description?: string,
    price?: number 
    categoryName?: string,
    providerId?: string,
    categoryId?: string,  
    isSelected?: boolean   
    image?: string    
  }